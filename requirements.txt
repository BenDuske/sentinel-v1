fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
reportlab>=4.0        # PDF report export (pulls in Pillow + charset-normalizer); license texts in licenses/

# --- Optional extras (not required to run; the app degrades gracefully without them) ---
# Markdown export always works; PDF export via reportlab (above) is now included by default.
#
# Dev/test (optional): `pip install -r requirements-dev.txt` (pytest + httpx),
# then run the keyless/offline suite with `python -m pytest -q`.
