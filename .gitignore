.env
sentinel.db
_smoke.db
evidence/
_smoke_evidence/
__pycache__/
*.pyc
.venv/

# test / coverage artifacts (machine-local, rewritten every run)
.coverage
.coverage.*
coverage.xml
htmlcov/
.pytest_cache/

# stray local export probes
_probe.pdf
*.probe.pdf

# Ben-clone voice narration — regenerate locally via scripts/generate_narration.py.
# NEVER commit these to this PUBLIC repo (voice-clone material is personal/sensitive).
media/narration/*.mp3
